# Google OAuth Credentials
# Obtén estas credenciales desde: https://console.cloud.google.com/apis/credentials
# Importante: El callbackURL debe ser: http://localhost:8080/auth/google/callback
GOOGLE_CLIENT_ID=tu-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-tu-client-secret

# MongoDB Atlas Connection
# Obtén tu conexión desde: https://cloud.mongodb.com
# Nota: Los caracteres especiales en la contraseña deben estar codificados en URL
MONGO_URI=mongodb+srv://usuario:password@cluster.mongodb.net/?appName=ClusterName

# Email Configuration (Gmail)
# EMAIL_USER: Tu dirección de Gmail
# EMAIL_PASS: Contraseña de aplicación de Gmail (genera en: https://myaccount.google.com/apppasswords)
EMAIL_USER=tu-email@gmail.com
EMAIL_PASS=tu-app-password-de-gmail

# Application URL
# En local usa: http://localhost:8080
# En producción (Google Cloud Run o similar), usa tu URL de producción
# Si no está definida, el sistema usa http://localhost:8080 por defecto
APP_URL=http://localhost:8080
